topology:
  nodes:
    - id: deployment
      metadata:
        description: "Storage associato alle applicazioni"
      ref:
        kind: "apps/*/Deployment"
        name: "my-*"
    - id: deployment2
      metadata:
        description: "test"
      ref:
        kind: "apps/*/Deployment"
        name: "deps"
      dependencies:
        - deployment
      
