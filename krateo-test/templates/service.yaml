apiVersion: v1
kind: Service
metadata:
  name: {{ include "krateo-test.fullname" . }}
  labels:
    {{- include "krateo-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krateo-test.selectorLabels" . | nindent 4 }}
