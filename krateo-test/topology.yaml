topology:
  nodes:
    - id: deployment
      metadata:
        description: "Storage associato alle applicazioni"
      ref:
        kind: "apps/*/Deployment"
        name: "my-*"
      dependencies:
        - deployment2

    - id: deployment2
      metadata:
        description: "test"
      properties:
        spec:
          disk:
            size: "100GB"
      ref:
        kind: "apps/*/Deployment"
        name: "deps"

      
